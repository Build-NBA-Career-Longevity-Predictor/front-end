import React from "react";

const Search = () => {
  //Search Form here
  //Maybe react-select to populate an dropdown input with all the players from existing database
  //Maybe chance to input your own player who's currently not in the database (will need to be able to input stats)
  return <div>Search</div>;
};

export default Search;
